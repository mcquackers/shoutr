<% if target.errors.any? %>
  <h3><%= pluralize target.errors.count, "error" %> prevented the form from saving. </h3>
  <ul>
    <% target.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
<% end %>
